window.onload = function() {
    document.getElementsByClassName("fadein").item(0).style.opacity = "1"
    document.getElementsByClassName("fadein").item(0).style.paddingTop = "0"
    startAnimation(); // for title on home screen
}